source=./theme.conf

$font = Symbols Nerd Font, Rubik

general {
  hide_cursor = true
  ignore_empty_input = true
  fail_timeout = 1000
}

background {
  monitor =
  path = screenshot
  blur_passes = 4
}

label {
  monitor =
  text = $LAYOUT
  color = $theme_foreground
  font_size = 16
  font_family = $font
  position = -30, 30
  halign = right
  valign = bottom
}

label {
  monitor =
  text = $TIME
  color = $theme_foreground
  font_size = 80
  font_family = $font
  position = -30, -20
  halign = right
  valign = top
}

label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $theme_foreground
  font_size = 20
  font_family = $font
  position = -30, -150
  halign = right
  valign = top
}

label { 
    monitor =
    text = $USER
    color = $theme_foreground
    font_size = 22
    font_family = $font
    position = 0, 50
    halign = center
    valign = center
}

input-field {
  monitor =
  rounding = 3
  size = 300, 54
  outline_thickness = 1
  dots_size = 0.10
  dots_spacing = 0.25
  dots_center = true
  outer_color = $theme_outline
  inner_color = $theme_input_bg
  font_color = $theme_foreground
  font_family = $font
  fade_on_empty = false
  hide_input = false
  check_color = $theme_accent
  fail_color = $theme_alert
  capslock_color = $theme_capslock
  position = 0, -25
  halign = center
  valign = center
  shadow_passes = 2
  shadow_color = $theme_shadow
  shadow_boost = 0.2
}

label {
    monitor =
    text = cmd[update:5000] ~/.config/hypr/hyprlock/status.sh

    color = $theme_foreground
    font_size = 16
    font_family = $font

    position = 30, 30
    halign = left
    valign = bottom
}
